{
  "name": "claude-code-plugin",
  "private": true,
  "version": "0.2.0",
  "description": "Prism Hub protocol-native Claude Code adapter built on the Claude Agent SDK",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/test/**/*.test.js"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "0.3.263",
    "@rokid-prism/pluginbridge-plugin-sdk": "^0.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  },
  "engines": {
    "node": ">=22 <23"
  },
  "license": "MIT"
}
